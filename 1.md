我要开发一个“Android 手机远程 Web 群控系统”。

请你作为这个项目的高级全栈/系统工程师，负责架构设计、代码实现、调试和后续迭代。

一、项目目标

我现在有一台 Windows 电脑，上面通过 USB/OTG 连接大约 40 台 Android 手机。

电脑端目前已经可以使用 scrcpy 查看和控制手机，例如：

```
scrcpy -s DEVICE_SERIAL
```

我希望在现有基础上增加一个 Remote Agent，使得：

浏览器
↓
Web 系统
↓
Remote Agent（Windows）
↓
USB
↓
Android 手机

最终用户不需要安装本地群控软件，只需要打开浏览器即可：

1. 登录 Web 系统
2. 查看所有 Android 设备
3. 查看设备在线/离线状态
4. 查看手机实时画面
5. 点击手机
6. 鼠标滑动
7. 键盘输入
8. 后续支持多设备群控
9. 后续支持用户权限和设备权限

目前不要一开始就做 40 台，也不要一开始接 VPS。

第一阶段只做：

1 台 Android
↓
Windows PC
↓
Remote Agent
↓
浏览器

先把完整链路跑通。

***

二、非常重要的技术原则

不要依赖我现在使用的第三方群控软件的“画面 API”。

因为现有群控软件虽然可以控制手机，但是没有可用的画面 API。

目前已经确认：

scrcpy 可以正常获取 Android 手机画面。

当前测试方式：

```
scrcpy -s XXXXX
```

可以正常打开手机窗口。

我的 scrcpy 版本是：

```
scrcpy 3.3.4
```

因此项目应该围绕 scrcpy / Android USB / Windows Remote Agent 来设计。

***

三、视频传输架构

最终目标：

Android
↓
USB
↓
scrcpy
↓
H.264 视频流
↓
Windows Remote Agent
↓
WebRTC
↓
Browser

不要采用：

Android
↓
scrcpy
↓
Windows 窗口
↓
截图
↓
重新编码
↓
浏览器

这种方案作为最终架构。

可以在 MVP 阶段临时使用窗口捕获验证整个链路，但最终应该尽量直接获取 scrcpy 的原始视频流，避免：

解码 → 截图 → 再编码

造成 CPU/GPU 浪费。

请你研究 scrcpy 3.3.4 的实际工作方式，并选择合理的实现方式。

注意：

不要假设 scrcpy 支持：

```
scrcpy > video.h264
```

或者：

```
scrcpy --stdout
```

如果 scrcpy CLI 本身不能直接输出视频，请研究 scrcpy client/server 协议、源码或其它合理方式。

***

四、最终系统架构

最终希望：

```
                Internet
                   │
                   ▼
            ┌──────────────┐
            │    VPS       │
            │              │
            │ Web Server   │
            │ WebSocket    │
            │ WebRTC Signaling
            │ Auth         │
            └──────┬───────┘
                   │
                   ▼
          ┌──────────────────┐
          │ Windows PC Agent │
          │                  │
          │ Device Manager   │
          │ Screen Stream    │
          │ Input Controller │
          └────────┬─────────┘
                   │
          USB      │
      ┌────────────┼────────────┐
      ▼            ▼            ▼
   Android 1    Android 2    Android 40
```

浏览器：

```
Browser
   │
   ├── HTTPS
   ├── WebSocket
   └── WebRTC
          ↓
         VPS
          ↓
     Windows Agent
          ↓
        Android
```

***

五、第一阶段 MVP

先不要做 VPS。

先在 Windows 本机实现：

```
Android
   ↓ USB
scrcpy / Agent
   ↓
Local Web Server
   ↓
Browser
```

浏览器访问：

```
http://127.0.0.1:PORT
```

能够看到：

```
Device 1
┌────────────────────┐
│                    │
│   Android Screen   │
│                    │
└────────────────────┘
```

并且能够：

- 实时显示手机画面
- 鼠标点击
- 鼠标按下/抬起
- 鼠标滑动
- 键盘输入
- Home
- Back
- Recent Apps

***

六、设备管理

Agent 启动后自动检测 USB Android：

例如：

```
adb devices
```

得到：

```
SERIAL_001
SERIAL_002
```

第一阶段只需要处理一个设备。

但是代码架构必须从一开始就支持多个 Device。

例如：

```
DeviceManager
    ├── Device 001
    ├── Device 002
    ├── Device 003
    └── ...
```

每一个 Device 对象至少包含：

```
serial
status
model
androidVersion
width
height
streamStatus
controlStatus
```

后续扩展到 40 台时，不应该重构整个项目。

***

七、视频要求

MVP：

单台手机：

```
720p
15~30 FPS
```

优先低延迟。

可以参考：

```
--max-size=720
--max-fps=15
--video-bit-rate=2M
```

但不要机械使用这些参数。

请根据实际性能测试调整。

未来 40 台：

缩略图模式：

```
320~568 左右
5~10 FPS
低码率
```

点击某台以后：

```
720p
15~30 FPS
```

即：

```
40 台低质量预览
+
1 台高清控制
```

而不是 40 台全部高码率。

***

八、WebRTC

优先考虑 WebRTC。

原因：

- 低延迟
- 浏览器原生支持
- 适合实时视频
- 后续扩展公网比较容易

请设计：

Browser
↕
WebRTC
↕
Remote Agent

VPS 第一阶段不参与媒体转发。

VPS 只负责：

- 用户认证
- Agent 注册
- WebSocket
- WebRTC signaling
- 权限
- 设备列表

后续根据 NAT 情况增加：

```
STUN
TURN
```

如果实际测试发现 P2P 不稳定，再引入 TURN。

***

九、控制链路

浏览器发送：

```
click
mouseDown
mouseMove
mouseUp
swipe
key
text
home
back
recent
```

例如：

```
{
    "type": "input",
    "device": "SERIAL_001",
    "action": "tap",
    "x": 500,
    "y": 900
}
```

Agent 收到以后：

```
Browser
   ↓
WebSocket
   ↓
Agent
   ↓
scrcpy / ADB / Android input
   ↓
Android
```

注意：

必须处理浏览器坐标和 Android 实际分辨率之间的转换。

例如：

浏览器显示：

```
360 × 640
```

手机实际：

```
1080 × 1920
```

那么：

```
x_android = x_browser / width_browser * width_android
y_android = y_browser / height_browser * height_android
```

如果手机旋转，也必须考虑。

***

十、Agent 要求

Remote Agent 运行在 Windows。

要求：

- 后台运行
- 自动发现 ADB 设备
- 自动连接
- 设备断开后自动恢复
- scrcpy 崩溃后自动重启
- 网络断开后自动重连服务器
- 不应该因为一台手机异常导致整个 Agent 崩溃

建议模块：

```
agent/
├── main
├── device-manager
├── device
├── scrcpy-manager
├── stream-manager
├── input-manager
├── websocket-client
├── webrtc
├── config
└── logger
```

***

十一、Web 前端

第一阶段可以使用：

React / Vue

你根据项目复杂度自行选择。

页面至少：

```
登录页

↓

Dashboard

┌─────────────────────────────┐
│ Device 001   ONLINE         │
│                             │
│     Android Screen          │
│                             │
│                             │
└─────────────────────────────┘
```

控制：

```
Home
Back
Recent
```

并支持：

```
鼠标点击
鼠标拖动
滚轮
键盘输入
```

***

十二、项目结构

你可以自行设计，但必须模块化。

推荐：

```
remote-phone-control/

├── agent/
│   ├── src/
│   ├── package.json
│   └── config/
│
├── server/
│   ├── src/
│   └── package.json
│
├── web/
│   ├── src/
│   └── package.json
│
├── shared/
│
├── docs/
│
└── README.md
```

如果你认为其它结构更合理，可以调整。

***

十三、安全

这个系统最终可以远程控制真实 Android 设备，所以安全必须从架构阶段考虑。

必须：

- HTTPS
- WSS
- Agent 身份认证
- 用户登录
- Session
- Token
- Device 权限
- 防止未授权控制
- 操作日志
- Agent 心跳
- 会话超时

绝对不要：

```
把 ADB 端口直接暴露到公网。
```

推荐：

Agent 主动连接 VPS：

```
Agent → WSS → VPS
```

而不是：

```
Internet → ADB
```

***

十四、开发方式

不要一次性写完整项目然后告诉我“完成”。

按照下面阶段推进：

Phase 1：

```
检查 Windows 环境
检查 ADB
检查 scrcpy
检查 Node.js
检查浏览器环境
```

Phase 2：

```
实现 DeviceManager
检测 Android
```

Phase 3：

```
实现单设备视频获取
```

Phase 4：

```
浏览器实时显示
```

Phase 5：

```
WebRTC
```

Phase 6：

```
鼠标控制
```

Phase 7：

```
键盘控制
```

Phase 8：

```
断线重连
```

Phase 9：

```
多设备
```

Phase 10：

```
VPS
```

Phase 11：

```
用户系统 / 权限
```

Phase 12：

```
40 台压力测试
```

每完成一个阶段：

1. 告诉我完成了什么
2. 告诉我修改了哪些文件
3. 告诉我如何运行
4. 告诉我如何测试
5. 如果出现问题，优先自己分析日志和修复
6. 不要跳过测试直接进入下一阶段

***

十五、非常重要

你现在不要直接开始大规模开发。

第一步只做：

【环境检查 + 架构确认】

请检查：

```
Windows
Node.js
npm
ADB
scrcpy
当前 Android 设备
```

然后告诉我：

1. 检测结果
2. scrcpy 3.3.4 是否适合
3. 你准备如何获取 scrcpy 视频流
4. MVP 准备使用什么 WebRTC 方案
5. 项目目录结构
6. Phase 1 需要创建哪些文件

确认方案以后，再开始写代码。

如果发现某个技术方案存在问题，不要硬写代码，先告诉我具体问题和替代方案。

整个项目目标是：

“让一台 Windows PC 上通过 USB 连接的 Android 手机，可以被浏览器实时查看和远程控制，并最终扩展到约 40 台设备。”\
严格遵守“先验证、后开发”的原则。

每一步都必须有可运行的最小结果。
不要为了最终架构过早引入复杂组件。

特别是视频链路：
先证明“scrcpy → Agent → Browser”能够稳定传输一台手机画面，再考虑 40 台、多用户、VPS、TURN 等问题。

如果你不确定某个 API、scrcpy 参数或底层协议是否存在，必须先通过本机实际检查、源码或官方文档确认，不允许凭经验编造 API。
